# NNNN — discussion

Append-only. Newest at the bottom, each entry dated. Options weighed, decisions, open questions, dead
ends — the thinking behind the spec. Keeps [`README.md`](README.md) clean.

- YYYY-MM-DD: …

- 2026-08-13: Built Hero (serif headline, lead, two CTAs → #work / #contact, location with MapPin, animated
  scroll cue + soft accent glow) and About (editorial two-column: sticky heading + lead statement, body
  paragraphs from `about.body` via returnObjects). Numbered eyebrows ("01 / About") for editorial rhythm.
- 2026-08-13: Verified — hero reads well in dark + light (screenshots); About renders lead + all 3
  paragraphs, opacity 1 after scroll, 0 console errors. Note: below-fold Reveal sections read as blank in a
  top-of-page full screenshot because whileInView hasn't fired — correct behavior, not a bug.
