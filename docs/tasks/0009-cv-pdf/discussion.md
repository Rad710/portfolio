# NNNN — discussion

Append-only. Newest at the bottom, each entry dated. Options weighed, decisions, open questions, dead
ends — the thinking behind the spec. Keeps [`README.md`](README.md) clean.

- YYYY-MM-DD: …

- 2026-08-13: Kept it simple per owner — no build pipeline. Authored two self-contained print-CSS CVs
  (`cv/cv-en.html`, `cv/cv-es.html`), exported once with headless Chrome to `public/cv/*.pdf`. Both one page,
  text-based, ATS-clean (single column, standard headings). ES uses slightly tighter spacing since Spanish
  runs longer.
- 2026-08-13: Header adds rad710.com + github.com/Rad710 + LinkedIn + phone; Projects list live + repo
  links (dyr-ai.rad710.com, rad710.pythonanywhere.com, GitHub). Accuracy fixes applied: Material UI (X Data
  Grid/Charts) not shadcn, added FastAPI, added the public text-to-sql-rag project.
- 2026-08-13: Verified both PDFs render correctly (visual check) and are exactly one page.
