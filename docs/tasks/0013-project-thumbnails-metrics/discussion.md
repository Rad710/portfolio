# NNNN — discussion

Append-only. Newest at the bottom, each entry dated. Options weighed, decisions, open questions, dead
ends — the thinking behind the spec. Keeps [`README.md`](README.md) clean.

- YYYY-MM-DD: …

- 2026-08-13: Captured screenshots from the live apps (registered a demo account on text-to-sql-rag →
  agentic SQL + result table, synthetic/mock data; DyR → login screen, interim until a demo instance with
  synthetic data is deployed on the VM).
- 2026-08-13: **Course-correction after review + reading a strong reference portfolio (Brittany Chiang).**
  The derived "metrics" (11 REST groups, 15 models, 80% coverage, 14 ADRs) were vanity counts — they read as
  junior and oversell a small personal RAG. Dropped the stat rows entirely and the 3-column
  Problem/Approach/Outcome case-study format. Replaced with the pattern good eng portfolios actually use:
  a screenshot in browser chrome + a confident 1-line tagline + a brief description (what it does), tech
  tags, live/code. Removed `problem/approach/outcome/stats` from the content model; added `description`.
- 2026-08-13: Verified — first featured card renders the framed screenshot (URL bar) + brief copy, no stats;
  build + biome clean; 0 console errors.
